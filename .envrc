# 1. Set the SDK path correctly
export VULKAN_SDK="$HOME/VulkanSDK/1.4.341.1/macOS"

# 2. Use the correct variable (VULKAN_SDK) for the rest
PATH_add "$VULKAN_SDK/bin"
export DYLD_LIBRARY_PATH="$VULKAN_SDK/lib"

# 3. Set the Driver (ICD) - Use VK_DRIVER_FILES (modern) instead of VK_ICD_FILENAMES
export VK_DRIVER_FILES="$VULKAN_SDK/share/vulkan/icd.d/libkosmickrisp_icd.json:$VULKAN_SDK/share/vulkan/icd.d/MoltenVK_icd.json"

# 4. Set the Layer path
export VK_LAYER_PATH="$VULKAN_SDK/share/vulkan/explicit_layer.d"